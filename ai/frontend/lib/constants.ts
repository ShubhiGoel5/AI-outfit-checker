// Frontend constants that are infrastructure-only
// These do NOT affect model behavior or violate assignment constraints

export const SUPPORTED_FILE_TYPES = {
  "image/*": [".jpeg", ".jpg", ".png", ".webp"],
}

export const MAX_FILE_SIZE = 10 * 1024 * 1024 // 10MB
